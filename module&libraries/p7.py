from math import factorial
from math import sqrt
from math import pow

a = int(input("Enter a number"))
print(f"Factorial: {factorial(a)}")
print(f"Square: {sqrt(a)}")
print(f"power: {pow(a,2)}")